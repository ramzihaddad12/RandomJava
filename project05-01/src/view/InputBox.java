package view;

import javax.swing.JFrame;

/**
 * An input box that is used for getting the inputs needed
 * to construct the model.
 */

public class InputBox extends JFrame {
  /**
   * A constructor for the InputBox.
   */
  public InputBox() {
    setSize(400, 300);
    setTitle("Setting up the dungeon");
  }
}